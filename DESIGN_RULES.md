# Правила дизайна проекта

## Заголовки

- Заголовки никогда не заканчиваются точкой
- Для нескольких частей заголовка использовать запятые, переносы строк или другое построение без точек
- Перед публикацией проверять это правило во всех `h1`–`h6` и заголовках модальных окон
